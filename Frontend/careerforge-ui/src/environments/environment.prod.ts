export const environment = {
  // TODO: replace with your real deployed backend URL once the
  // backend is hosted (e.g. Azure, Render, Railway). Until then,
  // a production build will fail to reach any API.
  apiUrl: 'https://your-deployed-backend-url.azurewebsites.net/api',
  googleClientId: 'YOUR_GOOGLE_OAUTH_CLIENT_ID.apps.googleusercontent.com'
};
